# 多平台文章发布系统开发规范

## 1. 系统架构概述

### 1.1 模块划分

```
agent/article/
├── config/          # 平台配置与格式规范
├── controller/      # API 控制器
├── dto/             # 数据传输对象
├── entity/          # 数据库实体
├── publish/         # 平台发布策略
│   └── impl/        # 各平台发布实现
├── repository/      # 数据访问层
└── service/         # 业务逻辑层
```

### 1.2 核心流程

1. **文章生成阶段**
   - 生成主题大纲
   - 生成标题（多平台适配）
   - 生成正文（含图片占位符）
   - 自动生成并插入插图

2. **格式转换阶段**
   - 按平台规范转换Markdown
   - 处理标题样式
   - 处理代码块格式
   - 处理段落长度

3. **发布阶段**
   - 平台格式校验
   - 自动发布到目标平台
   - 收集发布结果

---

## 2. 平台格式规范

### 2.1 各平台要求对比

| 平台 | 最低字数 | 最少图片数 | 支持代码块 | 支持表格 | 标题风格 |
|------|---------|-----------|-----------|---------|---------|
| CSDN | 1500 | 3 | ✅ | ✅ | 标准Markdown |
| 今日头条 | 2000 | 5 | ⚠️ 缩进式 | ❌ | 加粗文字 |
| 知乎 | 2500 | 4 | ✅ | ✅ | 标准Markdown |
| 掘金 | 1800 | 4 | ✅ | ✅ | 标准Markdown |
| 微信公众号 | 2000 | 6 | ❌ | ✅ | 编号标题 |
| B站专栏 | 1500 | 3 | ✅ | ✅ | 标准Markdown |

### 2.2 平台代码定义

```java
public enum PlatformType {
    CSDN("csdn", "CSDN博客"),
    TOUTIAO("toutiao", "今日头条"),
    ZHIHU("zhihu", "知乎"),
    JUEJIN("juejin", "掘金"),
    WECHAT("wechat", "微信公众号"),
    BILIBILI("bilibili", "B站专栏");
}
```

---

## 3. 文章结构规范

### 3.1 标准文章结构

```
# 文章标题

> 文章摘要（100-200字）

![封面](封面图URL)

## 引言

（背景介绍、问题陈述、本文目标，150字左右）

## 核心概念

### 3.1 基本定义

![核心概念示意图](图片URL)

（概念解释）

### 3.2 核心特性

- 特性一：xxx
- 特性二：xxx
- 特性三：xxx

## 技术原理

（深入讲解实现机制）

## 实践案例

### 4.1 环境准备

### 4.2 完整实现

```java
// 完整可运行的代码示例
public class Example {
    // 代码注释
}
```

## 最佳实践

1. xxx
2. xxx
3. xxx

## 总结

（100字左右，提炼核心要点）

**标签：** 标签1, 标签2, 标签3
```

### 3.2 图片插入规范

1. **封面图**：文章开头，16:9 比例
2. **章节配图**：每个主要章节标题下方
3. **图片说明**：使用章节标题作为图片描述
4. **占位符格式**：`[IMAGE: 章节标题 - 描述性文字]`

---

## 4. API 接口规范

### 4.1 文章生成接口

#### 生成指定平台的文章内容
```
POST /api/v1/agent-article/generate/content/platform/{platform}
Content-Type: application/json

{
  "title": "文章标题",
  "outline": "文章大纲（可选）",
  "keywords": ["关键词1", "关键词2"]
}

返回：
{
  "code": 200,
  "message": "success",
  "data": {
    "type": "content",
    "content": "生成的Markdown内容（含图片）",
    "model": "gpt-4o-mini",
    "tokens": 3500
  }
}
```

#### 生成完整文章（多平台适配）
```
POST /api/v1/agent-article/generate/full-article?platforms=csdn,toutiao,zhihu
Content-Type: application/json

{
  "title": "文章标题",
  "outline": "文章大纲",
  "keywords": ["关键词1", "关键词2"]
}
```

#### 获取支持的平台列表
```
GET /api/v1/agent-article/platforms

返回：
{
  "code": 200,
  "message": "success",
  "data": [
    {"code": "csdn", "name": "CSDN博客", "minWordCount": 1500, ...},
    ...
  ]
}
```

### 4.2 发布接口

#### 发布到指定平台
```
POST /api/article/publish/{articleId}
Content-Type: application/json

{
  "platforms": ["csdn", "toutiao", "zhihu"],
  "tags": ["Java", "Spring Boot"],
  "isOriginal": true
}

返回：
[
  {
    "platform": "CSDN",
    "success": true,
    "articleUrl": "https://blog.csdn.net/article/details/xxx",
    "articleId": "csdn-123",
    "publishedAt": "2024-01-01T12:00:00"
  },
  ...
]
```

#### 一键发布到所有平台
```
POST /api/article/publish/auto/{articleId}
```

#### 预览指定平台格式
```
GET /api/article/publish/preview/{articleId}/{platform}

返回：处理后的Markdown内容
```

#### 预览所有平台格式
```
GET /api/article/publish/preview/{articleId}

返回：
{
  "csdn": "CSDN格式的内容...",
  "toutiao": "头条格式的内容...",
  "zhihu": "知乎格式的内容..."
}
```

#### 校验文章是否符合平台要求
```
GET /api/article/publish/validate/{articleId}/{platform}

返回：true/false
```

---

## 5. 开发规范

### 5.1 新增平台发布实现

1. **创建发布器类**，继承 `AbstractPlatformPublisher`：

```java
@Component
public class XxxPublisher extends AbstractPlatformPublisher {
    
    public XxxPublisher(MarkdownProcessor markdownProcessor) {
        super(markdownProcessor);
    }

    @Override
    public PlatformType getPlatformType() {
        return PlatformType.XXX;
    }

    @Override
    protected PublishResult doPublish(PublishContext context) {
        // 实现平台特定的发布逻辑
        String articleId = "xxx-" + context.getArticle().getId();
        String url = getPublishUrl(articleId);
        
        // 调用平台API发布...
        
        return PublishResult.success(PlatformType.XXX, url, articleId);
    }

    @Override
    public String getPublishUrl(String articleId) {
        return "https://xxx.com/article/" + articleId;
    }
}
```

2. **在 `PlatformType` 枚举中添加新平台**
3. **在 `ArticleFormatConfig` 中配置格式规范**
4. **在 `ArticleGenerationService` 中添加平台Prompt**

### 5.2 Markdown格式转换扩展

如需添加新的格式处理逻辑：

1. 在 `ArticleFormatConfig` 中添加配置项
2. 在 `MarkdownProcessor` 中添加处理方法
3. 遵循现有处理流程：标题 → 代码块 → 列表 → 段落 → 其他

### 5.3 代码规范

#### 5.3.1 注释规范（强制要求）

**1. 类注释（JavaDoc）**
- 所有public类必须添加类注释，说明类的功能、职责、使用场景
- 包含@author、@since、@see等标签（如适用）
```java
/**
 * 病毒检测服务类
 * <p>提供系统安全扫描、AI智能分析、修复脚本生成等核心功能
 * <p>主要功能包括：
 * <ul>
 *   <li>系统进程扫描 - 检测可疑进程和恶意软件</li>
 *   <li>启动项检测 - 检查自启动程序</li>
 *   <li>服务检查 - 扫描系统服务异常</li>
 *   <li>AI智能分析 - 基于Spring AI进行安全评估</li>
 *   <li>修复脚本生成 - 自动生成Windows批处理修复脚本</li>
 * </ul>
 * 
 * @author System
 * @since 2025-01-01
 * @see VirusDetectorController
 * @see VirusScanResultDTO
 */
@Service
public class VirusDetectionService {
    // ...
}
```

**2. 方法注释（JavaDoc）**
- 所有public方法必须添加方法注释
- 说明方法功能、参数含义、返回值、异常抛出
```java
/**
 * 执行完整的系统安全扫描
 * <p>扫描内容包括：进程、启动项、服务、计划任务、开放端口、系统漏洞等
 * 
 * @param session Http会话对象，用于存储扫描结果
 * @return 扫描结果DTO，包含安全评分、统计数据、问题列表
 * @throws BusinessException 扫描过程中发生业务异常
 */
public VirusScanResultDTO scanSystem(HttpSession session) {
    // ...
}
```

**3. 字段注释**
- 常量字段必须添加注释说明用途
- 复杂业务字段添加行尾注释
```java
/**
 * AI病毒分析系统提示词 - 定义AI角色和输出格式规范
 */
private static final String AI_VIRUS_SYSTEM_PROMPT = "...";

private static final int MAX_SCAN_RETRIES = 3; // 最大扫描重试次数
```

**4. 代码块注释**
- 复杂逻辑块必须添加注释说明
- 关键算法、业务规则必须说明
```java
// 计算系统健康评分：满分100，每个高危问题扣10分，中危扣5分，低危扣2分
int healthScore = 100;
healthScore -= criticalCount * 10;
healthScore -= highCount * 5;
healthScore -= mediumCount * 2;
healthScore = Math.max(0, healthScore);
```

**5. TypeScript/前端注释规范**
- 所有接口、类型定义必须添加注释
- 组件props和state必须添加注释
- 复杂逻辑函数必须添加注释
```typescript
/**
 * 可疑程序数据结构
 * @property id - 唯一标识符
 * @property name - 程序/进程名称
 * @property path - 程序路径
 * @property riskLevel - 风险等级：CRITICAL/HIGH/MEDIUM/LOW/SAFE
 * @property category - 分类：PROCESS/STARTUP/SERVICE等
 */
export interface SuspiciousProgram {
  id: string;
  name: string;
  path: string;
  riskLevel: RiskLevel;
  category: ProgramCategory;
  // ...
}
```

**6. 注释禁用规则**
- ❌ 禁止无意义注释：`int a = 1; // 定义a等于1`
- ❌ 禁止注释掉的代码：`// oldCode();`
- ❌ 禁止拼写错误和语法错误的注释
- ✅ 注释必须清晰、准确、有价值

#### 5.3.2 日志规范（强制要求）

**1. 日志级别使用规范**
- `error`：系统错误、异常、关键功能失败，必须包含异常对象
- `warn`：警告信息、非预期但可恢复的情况、降级处理
- `info`：关键业务流程节点、重要操作、状态变更
- `debug`：调试信息、详细执行过程（生产环境可关闭）
- `trace`：最详细的追踪信息（极少使用）

**2. 日志格式规范**
```java
// ✅ 正确：使用占位符，包含上下文信息
log.info("开始执行系统安全扫描，会话ID: {}", session.getId());
log.error("系统扫描失败，会话ID: {}, 错误原因: {}", session.getId(), e.getMessage(), e);

// ❌ 错误：字符串拼接，性能差
log.info("扫描完成，找到" + count + "个问题");

// ❌ 错误：异常日志不包含异常对象，丢失堆栈信息
log.error("发生错误: " + e.getMessage());
```

**3. 业务日志必须包含的信息**
- 用户标识（如userId、sessionId）
- 操作类型（如scan、analyze、generateScript）
- 关键参数（如platform、articleId）
- 执行结果（成功/失败、数据统计）
- 耗时（可选，用于性能监控）

**4. 日志示例**
```java
// 方法入口日志
log.info("【病毒检测】开始系统扫描，会话ID: {}", sessionId);
long startTime = System.currentTimeMillis();

try {
    // 业务逻辑...
    log.debug("【病毒检测】进程扫描完成，发现{}个可疑进程", processCount);
    
    // 方法成功结束日志
    long duration = System.currentTimeMillis() - startTime;
    log.info("【病毒检测】系统扫描完成，会话ID: {}, 耗时: {}ms, 发现问题: {}个", 
             sessionId, duration, totalIssues);
             
} catch (Exception e) {
    // 异常日志
    long duration = System.currentTimeMillis() - startTime;
    log.error("【病毒检测】系统扫描失败，会话ID: {}, 耗时: {}ms, 错误: {}", 
              sessionId, duration, e.getMessage(), e);
    throw e;
}
```

**5. 日志禁用规则**
- ❌ 禁止打印敏感信息：密码、密钥、token、用户隐私数据
- ❌ 禁止在循环中打印大量日志
- ❌ 禁止只打日志不处理异常（除非明确标记为吞异常）
- ❌ 禁止使用System.out.println代替日志框架

#### 5.3.3 异常处理规范

```java
// ✅ 正确：抛出业务异常
throw new BusinessException(ErrorCode.SCAN_FAILED, "系统扫描超时");

// ✅ 正确：捕获并包装异常
try {
    aiService.analyze(data);
} catch (AIServiceUnavailableException e) {
    log.warn("AI服务不可用，切换到降级模式", e);
    return fallbackAnalysis(data);
}

// ❌ 错误：捕获后不处理
try {
    riskyOperation();
} catch (Exception e) {
    // 空catch块，无声失败
}
```

#### 5.3.4 参数校验规范

- 使用 `@Valid` 和 `@NotBlank` 等注解进行入参校验
- 业务逻辑层也要做参数校验，不依赖Controller层校验
- 校验失败抛出明确的业务异常

#### 5.3.5 事务管理规范

- 写操作必须加 `@Transactional`
- 避免长事务，事务范围尽可能小
- 合理设置事务传播行为和回滚规则

---

## 6. 部署配置

### 6.1 依赖配置

在 `pom.xml` 中确保以下依赖：

```xml
<dependency>
    <groupId>org.commonmark</groupId>
    <artifactId>commonmark</artifactId>
    <version>0.21.0</version>
</dependency>
```

### 6.2 应用配置

```yaml
agent:
  platform:
    default-model: openai
    ai:
      fallback-enabled: true
      
spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: https://api.openai.com
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.7
```

---

## 7. 测试用例规范

### 7.1 单元测试覆盖点

1. **MarkdownProcessor**
   - 各平台标题格式转换
   - 代码块格式处理
   - 段落分割逻辑
   - 字数统计准确性
   - 图片数量统计

2. **ImageGenerationService**
   - 图片插入位置正确性
   - 占位图生成逻辑
   - 图片验证逻辑

3. **PublishService**
   - 多平台发布流程
   - 发布结果聚合
   - 异常处理逻辑

### 7.2 集成测试场景

1. 完整文章生成 + 多平台格式转换
2. 文章发布全流程
3. 降级模式下的功能可用性

---

## 8. 扩展开发指南

### 8.1 添加新的内容生成类型

1. 在 `GenerateService` 接口添加方法
2. 在 `GenerateServiceImpl` 实现
3. 添加对应的系统Prompt
4. 在Controller中暴露API

### 8.2 自定义文章结构

通过 `ArticleStructure` 类定义自定义结构：

```java
ArticleStructure structure = ArticleStructure.builder()
    .title("自定义标题")
    .sections(List.of(
        Section.builder()
            .heading("自定义章节")
            .level(2)
            .content("章节内容")
            .imageUrl("图片URL")
            .build()
    ))
    .build();
```

---

## 9. 常见问题

### 9.1 平台API对接

各平台开放平台地址：
- CSDN: https://open.csdn.net/
- 今日头条: https://developer.toutiao.com/
- 知乎: https://www.zhihu.com/developer
- 掘金: https://juejin.cn/api

### 9.2 图片生成失败

- 检查DALL-E API配额
- 自动降级使用占位图
- 支持用户手动上传替换

### 9.3 字数不达标

- 生成时指定目标字数
- 提供内容扩充功能
- 发布前校验并提示
